{
  "name": "snake",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "start": "npx parcel index.html",
    "build": "npm run clean && npx parcel build index.html",
    "build-gh": "npm run clean && npx parcel build index.html --public-url https://mct-dev.github.io/snake",
    "push-to-gh-pages": "git subtree push --prefix dist/ origin gh-pages"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "eslint": "^4.18.2",
    "eslint-plugin-react": "^7.7.0",
    "parcel-bundler": "^1.6.2"
  }
}
